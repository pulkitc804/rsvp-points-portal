{
  "name": "rsvp-points-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backend for the RSVP Points Portal",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test 'test/*.test.mjs'"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
